import styled from 'styled-components';

export const Field = styled.div`
    display: flex;
    align-items: center;

    background: rgba(255, 255, 255, 0.2);

    width: 100%;

    padding: 0 .7rem;

    border-radius: .5rem;

    overflow: hidden;
`;